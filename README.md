# currency_converter
A responsive web-based currency converter that fetches real-time exchange rates using a public API. Features include multi-currency support, theme switching, conversion history, default currency preferences, offline detection, and persistent local storage.
